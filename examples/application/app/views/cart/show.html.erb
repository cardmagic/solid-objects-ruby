<%= actor_scope @cart do |cart| %>
  <span>
    Cart items:
    <%= cart.value :items_count %>
  </span>

  <span>
    Subtotal:
    <%= cart.value :subtotal_cents %>
  </span>

  <%= cart.component :summary %>
<% end %>
