<%= actor_scope @cart do |cart| %>
  <span>
    Cart items:
    <%= cart.items_count %>
  </span>

  <span>
    Subtotal:
    <%= cart.subtotal_cents %>
  </span>

  <%= cart.component :summary %>
<% end %>
